<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.ibitu.persistence.MemberMapper">

<insert id="createAccount"> insert into tbl_user(uid, upw, uname) values(#{uid}, #{upw}, #{uname}) </insert>

</mapper>